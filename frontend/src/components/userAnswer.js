class UserAnswer {
  constructor(user_id, round_id, question_id, user_input, correct_answer) {
    this.user_id = user_id
    this.round_id = round_id
    this.question_id = question_id
    this.user_input = user_input
    this.correct_answer = correct_anwer
  }
}
